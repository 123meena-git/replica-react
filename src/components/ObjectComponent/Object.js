/*import './Obect.css';
import { continents, currencies,continents2 } from './data';

function Object () {
return (
    <div className='chart'>
        {continents.map((continent) => (<h3>{continent}</h3>))}
    </div>
);
 }
 export default Object;
 function Object ()*/